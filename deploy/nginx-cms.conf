# Nginx configuration for cms.arabiq.tech (Strapi CMS)

server {
    listen 80;
    server_name cms.arabiq.tech;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name cms.arabiq.tech;

    # SSL - will be added by certbot
    ssl_certificate /etc/letsencrypt/live/cms.arabiq.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cms.arabiq.tech/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/cms.arabiq.tech/chain.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Max upload size for Strapi media
    client_max_body_size 256M;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # Proxy to Strapi container
    location / {
        proxy_pass http://127.0.0.1:1337;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300;
    }

    # Health check
    location /health {
        access_log off;
        proxy_pass http://127.0.0.1:1337;
    }

    access_log /var/log/nginx/cms.arabiq.tech.access.log;
    error_log /var/log/nginx/cms.arabiq.tech.error.log;
}
