// manual-verify.js removed — project uses Strapi instead of Prisma.
// Use Strapi admin UI or `apps/cms/seed.mjs` to create test users and approvals.

console.log('manual-verify.js removed (Prisma dependency). Use `apps/cms/seed.mjs` or Strapi admin UI to create test users and roles.');